---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: mongodb-backup
  namespace: mongodb
spec:
  schedule: "5 1 * * *"
  successfulJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: mongodb-backup
            image: docker.io/mongo:8.0.26
            imagePullPolicy: IfNotPresent
            env:
              - name: HOSTNAME
                value: mongodb-svc.mongodb.svc.cluster.local
              - name: USERNAME
                valueFrom:
                  secretKeyRef:
                    name: mongodb-graylog-graylog
                    key: username
              - name: PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: mongodb-graylog-graylog
                    key: password
              - name: DATABASE
                value: graylog
            command: ["/bin/sh", "-c"]
            args:
              - /usr/bin/mongodump --uri="mongodb+srv://$USERNAME:$PASSWORD@$HOSTNAME/$DATABASE?replicaSet=mongodb&ssl=false" --archive=/backups/mongodb/mongodb-$(date +'%Y-%m-%d').archive;
                find /backups/mongodb -mtime +7 -delete;
            volumeMounts:
            - name: backups
              mountPath: /backups
          restartPolicy: Never
          volumes:
          - name: backups
            hostPath:
              path: "/mnt/backups"