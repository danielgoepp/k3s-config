---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: k3s-prod-ingress-tcp
  namespace: default
spec:
  entryPoints:
    - api-tcp
  routes:
  - match: HostSNI(`*`)
    services:
      - name: kubernetes
        port: 443